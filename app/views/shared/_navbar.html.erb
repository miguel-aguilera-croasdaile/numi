<div class="navbar">
  <div class="navbar-left">
    <%= link_to root_path, class: "navbar-brand" do %>
      <b>numi</b>
    <% end %>
  </div>
  <div class="navbar-right">

    <% unless current_page?(root_path) %>
      <div class="nav-item">
        <b><%= link_to "Explore", raffles_path, class: "nav-link" %></b>
      </div>
    <% end %>

    <% unless current_page?(root_path) %>
      <div class="nav-item">
        <b><%= link_to "Create Raffle", new_raffle_path, class: "nav-link" %></b>
      </div>
    <% end %>

    <% if current_user %>
      <div class="nav-item">
        <b><%= link_to "My Orders", orders_path, class: "nav-link" %></b>
      </div>
    <% end %>

    <% if current_user %>
    <div class="nav-item">
      <b><%= link_to "Log Out", destroy_user_session_path, class: "nav-link", :method => :delete %></b>
    </div>
    <% end %>

    <% unless current_user %>
    <div class="nav-item">
      <b><%= link_to "Log In", new_user_session_path, class: "nav-link" %></b>
    </div>
    <% end %>

  </div>
</div>
